# Changesets

This directory stores release notes entries used by Changesets.

Create a changeset before merge:

```bash
pnpm changeset
```

Apply pending version bumps:

```bash
pnpm version-packages
```

Publish packages:

```bash
pnpm release
```
